# PLANS.md

## 1. 文档目的

本文件用于约束本仓库内复杂任务的计划编制方式。

当任务涉及协议、架构、跨模块联动、认证安全、状态机、构建发布、桌面端与运行时协同等高风险内容时，代理或研发人员不得直接进入实现，而应先输出一份简明、可执行、可验证的实施计划。

本文件与仓库根 `AGENTS.md` 配合使用：

- `AGENTS.md` 约束工作边界与行为方式
- `PLANS.md` 约束复杂任务的分析与实施计划格式

---

## 2. 适用范围

满足以下任一条件时，默认必须先产出计划，再进入实现：

### 2.1 跨模块任务
涉及两个及以上模块，例如：

- `ltfp/` 与 `agent-core/`
- `agent-core/` 与 `cloud-bridge/`
- `cloud-bridge/` 与 `apps/dev-agent/`
- `service-registry/` 与其他模块
- 后端运行时与前端管理台联动

### 2.2 高风险任务
涉及以下任一内容：

- 协议结构调整
- 编解码行为变化
- 会话状态机调整
- transport 抽象调整
- 路由/发现/注册行为调整
- 认证、鉴权、证书、安全设计
- Tauri 与 Agent Core 本地通信
- 配置结构或默认值语义变化
- 构建、发布、回滚链路变化
- 兼容性策略变化

### 2.3 影响难以直观看清的任务
例如：

- 需要阅读多个文档后才能动手
- 需要同步修改测试、文档和代码
- 可能引发旧版本不兼容
- 容易出现“局部看似正确、系统整体出问题”的改动

---

## 3. 哪些任务可以不写计划

满足以下条件时，可直接进入实现，但仍应进行基本分析：

- 明确的单点 bug 修复
- 注释、文案、文档排版修正
- 单个测试补充
- 不改变行为语义的小范围重构
- 明确局限于单目录、单文件、单函数的低风险改动

即便如此，也仍需在输出中说明：

- 修改目标
- 影响范围
- 验证情况

---

## 4. 计划编制的核心原则

### 4.1 计划必须服务于实施
计划不是“写一篇长文档”，而是用于降低误改、漏改、错改的风险。  
计划必须回答以下问题：

1. 为什么要改
2. 改哪里
3. 哪些不能乱改
4. 风险在哪
5. 怎么验证
6. 失败后如何回退

### 4.2 计划必须贴近当前仓库
计划必须基于本仓库实际目录和设计文档，不得凭空抽象。  
优先参考：

- `docs/Agent-BridgeSecurityArchitectureAndAuthDesign.md`
- `docs/Agent-and-Bridge-Implementation-Technical-Design.md`
- `docs/Agent–TauriLocalCommunicationDesignProposal.md`
- `docs/BridgeAdminBackendTechnicalProposal.md`
- `docs/DevBridge-MultiAgent-ServiceModel-TechDesign.md`
- `docs/LTFP-v1-Draft.md`
- `docs/LTFP-TransportAbstraction.md`
- `docs/LTFP-TunnelMultiplex-Proposal.md`
- `docs/task_plan.md`
- `docs/progress.md`
- `ltfp/docs/ReleaseAndRollback.md`
- `ltfp/docs/TestMatrix.md`

### 4.3 计划优先保证最小影响面
计划应优先选择：

1. 局部修改
2. 兼容式修改
3. 可回退修改
4. 可逐步验证修改

不要一上来选择“大一统重构”。

---

## 5. 计划的标准结构

复杂任务的计划默认应包含以下部分。

---

### 5.1 背景

说明为什么要做这项修改，至少包含：

- 当前问题是什么
- 问题出现在哪个模块或边界
- 现有方案为什么不够
- 本次计划解决到什么程度

---

### 5.2 目标

用明确、可判断的语言说明目标。  
目标应避免空泛表述，例如不要只写“优化体验”“完善架构”。

应写成类似：

- 让 Agent 与 Bridge 的会话握手行为在并发重连场景下保持幂等
- 为 `apps/dev-agent` 增加稳定的本地状态展示与错误反馈
- 将 `ltfp/transport` 中的控制通道优先级策略改为可配置实现，同时保持旧行为兼容

---

### 5.3 非目标

必须明确说明本次**不做什么**。  
这是控制范围最关键的一节。

例如：

- 不重写整个协议栈
- 不修改历史消息结构
- 不重构全部 UI 组件
- 不引入新的注册中心实现
- 不在本期替换现有构建系统

---

### 5.4 影响范围

说明会影响哪些模块、目录、文件类型或运行边界。

至少应写清楚：

- 主要影响模块
- 次级影响模块
- 是否涉及文档同步
- 是否涉及测试同步
- 是否涉及配置或发布链路

---

### 5.5 现状分析

从实现与架构两个层面说明现状：

- 当前调用链或控制链路是什么
- 当前状态流转是什么
- 当前存在的核心问题是什么
- 风险点出现在哪些边界

这一节不要泛泛而谈，要尽量贴近仓库实际结构。

---

### 5.6 方案候选与取舍

对于中高复杂度任务，建议至少写出 2 个方案，并说明为何选择当前方案。

至少要说明：

- 方案 A 是什么
- 方案 B 是什么
- 各自优劣
- 最终为什么选当前方案

如果任务很明确，也可以简化为：

- 当前方案
- 为什么不采用更重的方案

---

### 5.7 实施步骤

实施步骤必须可执行，建议拆成阶段。

每个阶段至少包含：

- 做什么
- 改哪里
- 产出是什么
- 风险点是什么
- 完成标志是什么

推荐写法：

1. 第一阶段：补充边界分析与必要文档确认
2. 第二阶段：修改核心实现
3. 第三阶段：补充测试与最小验证
4. 第四阶段：同步文档与收尾

---

### 5.8 兼容性分析

如果涉及以下内容，必须单独写兼容性分析：

- 协议
- 状态机
- 配置文件
- API / IPC / SSE
- Agent/Bridge 协同行为
- Tauri 本地通信
- 构建与发布链路

至少说明：

- 是否向后兼容
- 哪些旧行为保持不变
- 哪些行为会有新分支
- 旧版本如何处理
- 是否需要门控、回退或迁移逻辑

---

### 5.9 风险分析

至少说明：

- 功能风险
- 兼容性风险
- 并发或生命周期风险
- 性能风险
- 测试覆盖不足风险
- 发布或联调风险

风险分析不需要夸张，但必须真实。

---

### 5.10 验证计划

这一节必须具体，不能只写“完成后测试”。

应写清楚：

- 需要运行哪些测试
- 需要做哪些模块级构建验证
- 是否需要联调
- 是否需要人工验证界面
- 是否需要验证旧版本兼容场景
- 哪些验证当前阶段可能无法完成

---

### 5.11 回滚/降级策略

对于高风险任务，必须说明失败后怎么办。

至少说明：

- 能否直接回退代码
- 是否需要保留旧逻辑开关
- 是否存在双写/双读/双路径过渡期
- 发布后若异常，先回滚哪一层

---

### 5.12 文档同步项

说明本次变更是否需要同步更新：

- `docs/` 下技术方案
- `ltfp/docs/`
- README
- 执行清单
- 测试矩阵
- 发布说明

---

## 6. 计划输出模板

复杂任务默认使用以下模板：

### 6.1 标准模板

```text
# 任务计划标题

## 背景
...

## 目标
...

## 非目标
...

## 影响范围
...

## 现状分析
...

## 方案与取舍
...

## 实施步骤
### 阶段一
...
### 阶段二
...
### 阶段三
...

## 兼容性分析
...

## 风险分析
...

## 验证计划
...

## 回滚/降级策略
...

## 文档同步项
...
```

---

## 7. 面向本仓库的专项要求

### 7.1 涉及 `ltfp/`

必须特别回答：

* 是否影响消息结构
* 是否影响旧版本兼容
* 是否影响状态机
* 是否影响 transport 行为
* 是否需要更新 `ltfp/docs/TestMatrix.md`

### 7.2 涉及 `agent-core/`

必须特别回答：

* 是否影响启动/重连/关闭生命周期
* 是否影响协议探测
* 是否影响 Agent 与 Bridge 的交互
* 是否需要真实网络或联调验证

### 7.3 涉及 `cloud-bridge/`

必须特别回答：

* 是否影响 Agent 接入语义
* 是否影响路由/资源分配
* 是否影响配置兼容性
* 是否影响前后端契约

### 7.4 涉及 `apps/dev-agent/`

必须特别回答：

* 是否影响 UI 状态含义
* 是否影响 Tauri 宿主命令边界
* 是否影响与 Agent Core 的本地通信
* 是否需要桌面端实机验证

### 7.5 涉及 `service-registry/`

必须特别回答：

* 是否影响注册与订阅语义
* 是否影响 watcher/client 调用方
* 是否影响基础类型兼容性

---

## 8. 计划质量要求

一份合格计划至少满足：

1. 能看出问题边界
2. 能看出影响模块
3. 能看出为什么这样拆步骤
4. 能看出验证与回滚思路
5. 不依赖“实现时再看情况”作为主要策略

以下情况视为不合格：

* 只有空泛目标，没有实施步骤
* 没有非目标，导致边界失控
* 没有兼容性分析
* 没有验证计划
* 没有风险说明
* 没有贴近当前仓库实际结构

---

## 9. 执行过程中的计划维护

若计划已经开始执行，出现以下情况时，应更新计划：

* 实际影响范围扩大
* 发现现状与原判断不一致
* 验证项发生变化
* 需要新增回滚策略
* 发现文档约束与实现冲突

更新计划时，优先补充：

* 新发现
* 范围变更
* 风险变更
* 验证变更

不要静默偏离原计划。

---

## 10. 完成后的结果总结要求

执行完计划后，结果总结至少应包含：

1. 最终修改目标
2. 实际影响范围
3. 与原计划相比是否有偏差
4. 核心实现点
5. 风险与兼容性说明
6. 已执行验证
7. 未覆盖验证
8. 已同步或待同步的文档项

---

## 11. 简化决策原则

当存在多个可行方案时，优先级如下：

1. 正确性
2. 兼容性
3. 最小影响面
4. 可验证性
5. 可回退性
6. 实现优雅程度

若某个“更优雅”的方案显著增加跨模块风险，则不优先采用。

---

## 12. 本文件的使用方式

本文件不是要求所有任务都写大文档，而是要求：

* 高风险任务必须先有计划
* 计划必须能指导实施
* 计划必须贴近本仓库真实结构
* 计划必须服务于验证与回滚

对于复杂问题，先计划，再实现；
对于简单问题，先分析，再动手。

````

