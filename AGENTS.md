# AGENTS.md

## 1. 文档目的

本文件用于约束本仓库内智能编码代理（如 Codex）在分析、修改、生成、重构和评审代码时的工作方式。

本仓库是一个**单仓库、多模块、多运行时、多前后端子项目**的工程，包含：

- Go 后端服务
- Go 协议库
- Tauri 桌面端
- Web 管理界面
- 技术方案与执行清单文档
- 本地二进制与运行示例

代理在开始任何工作前，必须先识别目标模块、边界、依赖影响面和验证范围，避免跨模块误伤。

---

## 2. 总体工作原则

### 2.1 先分析，再修改
对于任何非微小改动，先完成以下动作：

1. 确认目标模块
2. 确认影响边界
3. 确认是否涉及协议、状态机、认证、安全、兼容性、构建发布或 UI 联动
4. 必要时先阅读相关 `docs/` 文档
5. 给出简要实施思路后再改代码

### 2.2 优先局部修改
若问题可以在所属模块内解决，则不要为了“看起来更优雅”而随意抽取公共层、重排目录或发起跨模块重构。

### 2.3 保持兼容性
本仓库存在明显的协议演进、Agent/Bridge 协同、前后端联动与本地通信设计。  
除非任务明确要求，否则不得主动引入以下破坏性变更：

- 协议字段语义变化
- 消息结构不兼容
- 状态机行为变化
- 认证流程变化
- 数据结构对外行为变化
- CLI / 配置文件不兼容
- Web / Tauri 交互协议不兼容

### 2.4 输出使用中文
除非任务明确要求英文，否则分析、说明、总结、评审意见、风险提示均使用中文输出。

---

## 3. 仓库结构认知

代理在工作时应将本仓库理解为以下几个主要区域：

### 3.1 `agent-core/`
Agent 核心运行时与相关基础能力，包含：

- `cmd/agent-core`：可执行入口
- `runtime/agent`：Agent 运行时核心逻辑
- `pkg/events`：事件定义
- `pkg/protodetect`：协议探测相关
- `pkg/tsbase62`：标识/编码类基础能力

### 3.2 `cloud-bridge/`
Bridge 服务端实现，包含：

- `cmd/cloud-bridge`：可执行入口
- `runtime/bridge`：Bridge 核心运行时
- `web/`：Bridge 管理界面前端及嵌入资源

### 3.3 `ltfp/`
LTFP 协议库，是仓库中的**高风险核心模块**，涉及：

- 协议模型
- 编解码
- 兼容性
- 一致性机制
- 发现与路由
- 会话状态机
- transport 抽象
- pb / proto
- 校验与版本控制
- 测试基线与发布验证

该模块的任何改动都必须慎重处理兼容性、状态一致性和测试覆盖。

### 3.4 `service-registry/`
面向注册发现的独立模块，负责与 Agent Core 或其他注册能力协作。  
对其修改时应避免将运行时细节直接耦合进上层业务。

### 3.5 `apps/dev-agent/`
Tauri 桌面端项目，包含：

- 前端界面
- Tauri Rust 宿主
- 本地 UI 资源
- 测试目录

该模块的修改通常涉及：

- 前端交互
- 本地通信
- UI 状态
- 与 Agent Core 的协同

### 3.6 `docs/`
项目设计、执行清单、技术方案、进度记录、调研结论所在目录。  
对于复杂需求、架构问题、协议设计问题、Agent/Bridge 协同问题，应优先阅读相关文档，而不是直接猜测实现。

### 3.7 `bin/`
本地二进制和运行样例目录。  
默认视为**构建产物 / 本地运行产物 / 演示资源**，除非任务明确要求，否则不要直接修改其中内容。

---

## 4. 模块边界规则

### 4.1 修改前必须先判断属于哪一类任务
任务必须先归类为以下三类之一：

1. **单模块局部修改**
2. **单模块 + 公共库联动修改**
3. **跨模块协同修改**

若属于第 1 类，则不得无关触碰其他模块。  
若属于第 2 或第 3 类，则必须在输出中明确说明影响模块。

### 4.2 公共库不是兜底垃圾桶
以下目录/模块必须谨慎抽象，不得为了减少几行重复代码就强行公共化：

- `ltfp/`
- `agent-core/pkg/`
- `service-registry/`

原则：

- 先保证边界正确，再考虑复用
- 只为真实跨模块复用抽象公共层
- 不制造“看起来通用、实际只服务一个调用点”的共享包

### 4.3 目录归属优先
实现应尽量放在所属模块附近：

- Agent 逻辑优先留在 `agent-core/`
- Bridge 逻辑优先留在 `cloud-bridge/`
- 协议抽象与通用校验优先留在 `ltfp/`
- 注册发现逻辑优先留在 `service-registry/`
- 桌面端 UI/交互优先留在 `apps/dev-agent/`
- Bridge Web 管理界面优先留在 `cloud-bridge/web/`

---

## 5. 高风险区域特别规则

---

### 5.1 `ltfp/` 协议库

`ltfp/` 是高风险核心区。修改该目录时，必须显式检查以下问题：

1. 是否破坏现有消息兼容性
2. 是否影响旧版本行为
3. 是否改变状态机推进条件
4. 是否引入重放、乱序、幂等性问题
5. 是否改变认证/协商/回退策略
6. 是否影响 transport 层抽象稳定性
7. 是否需要更新相关测试、文档和发布说明

尤其是以下子目录，默认按高风险处理：

- `codec/`
- `consistency/`
- `discovery/`
- `negotiation/`
- `pb/`
- `registry/`
- `routing/`
- `runtime/`
- `session/`
- `transport/`
- `validate/`

#### 对 `proto/`、`pb/`、`pb/gen/` 的规则
- 不要手工修改生成产物，除非任务明确要求
- 若涉及协议变更，优先修改源定义与生成流程
- 若生成代码需更新，应说明生成方式和验证方式

#### 对 transport / session / consistency 的规则
涉及以下内容时，必须特别说明风险：

- 会话建立与断连重连
- ack / replay / full sync
- heartbeat / queue / fragmentation
- tunnel / pool / control channel
- fallback / negotiation / versioning

---

### 5.2 `agent-core/`

修改 `agent-core/` 时，必须注意：

- 不要把 Agent 运行时逻辑拆散到无关目录
- `runtime/agent` 的生命周期、连接管理、事件分发、资源清理必须清晰
- `pkg/protodetect` 涉及协议探测时，应关注性能、误判、超时和回退
- 任何与 `ltfp` 协议层的耦合变更，都必须检查调用点和兼容性

若涉及并发、长连接、资源回收、状态同步，必须显式说明是否检查了：

- goroutine 生命周期
- channel/锁使用
- timeout / deadline
- 错误路径
- 重连和关闭流程

---

### 5.3 `cloud-bridge/`

修改 `cloud-bridge/` 时，必须注意：

- Bridge 是控制面/路由/会话协同中心，不能轻易改变行为语义
- `runtime/bridge` 中任何连接管理、认证、路由分配、会话绑定的改动，都属于高风险
- 如果改动涉及 Agent/Bridge 配合，必须同步评估 Agent 侧影响
- `config.example.yaml` 修改时要检查是否影响兼容性和默认行为

#### `cloud-bridge/web/`
这是 Bridge 的前端管理界面与嵌入资源目录。  
在该目录下工作时：

- 先复用现有 UI 模式与组件风格
- 避免无关的大范围视觉重构
- 修改前端接口时，必须检查与后端契约是否一致
- 若涉及 `embed.go`，注意资源构建和嵌入链路是否受影响

---

### 5.4 `apps/dev-agent/`

该目录是桌面端项目，包含前端与 Tauri 宿主。  
修改时需注意：

- UI 展示层与底层 Agent Core 通信边界清晰
- 不要把大量业务状态隐藏在视图层
- 不要轻易改动 Tauri 与本地进程通信协议，除非任务明确要求
- 若涉及本地 IPC、Named Pipe、Domain Socket、命令调用或事件桥接，必须说明影响面
- 若仅为功能修复，不要顺手重构整套界面或状态管理方案

---

### 5.5 `service-registry/`

修改该模块时必须注意：

- 保持注册发现职责单一
- 避免引入过重的运行时依赖
- 若改动 registry/watcher/client 行为，需检查调用侧兼容性
- 若变更基础类型，必须检查序列化和跨模块影响

---

## 6. 默认禁止的修改

除非任务明确要求，否则禁止：

### 6.1 修改构建产物或依赖目录
不要直接修改以下目录中的内容：

- `apps/dev-agent/dist`
- `apps/dev-agent/node_modules`
- `cloud-bridge/web/dist`
- `cloud-bridge/web/node_modules`
- `bin/`

### 6.2 修改本地二进制
不要直接修改以下文件或其同类产物：

- `*.exe`
- `*.dll`

### 6.3 直接改生成文件替代改源文件
对于以下内容，应优先改源而非直接改结果：

- 代码生成产物
- 嵌入资源产物
- 构建输出目录
- proto/pb 生成结果

### 6.4 擅自修改配置、发布和运行样例
以下属于高敏感内容，除非任务明确要求，否则不要改：

- `config.example.yaml`
- `bridge.yaml`
- 构建/发布脚本
- 本地运行样例
- 任何可能影响演示环境或发布流程的文件

---

## 7. 文档优先规则

对于以下任务，必须优先阅读 `docs/` 和相关模块文档：

- 协议设计/评审
- Agent 与 Bridge 交互问题
- 认证与安全设计
- Tauri 与 Agent 通信
- 管理后台设计
- 多 Agent 服务模型
- LTFP 执行清单/演进方案
- 任务拆解/实施计划/进度衔接

优先关注但不限于以下文档：

- `docs/Agent-BridgeSecurityArchitectureAndAuthDesign.md`
- `docs/Agent-and-Bridge-Implementation-Technical-Design.md`
- `docs/Agent–TauriLocalCommunicationDesignProposal.md`
- `docs/BridgeAdminBackendTechnicalProposal.md`
- `docs/BridgeAdminSSEProtocol.md`
- `docs/DevBridge-MultiAgent-ServiceModel-TechDesign.md`
- `docs/LTFP-v1-Draft.md`
- `docs/LTFP-TransportAbstraction.md`
- `docs/LTFP-TunnelMultiplex-Proposal.md`
- `docs/task_plan.md`
- `docs/progress.md`
- `ltfp/docs/ReleaseAndRollback.md`
- `ltfp/docs/TestMatrix.md`

如果已有文档明确了约束，应优先遵循文档，不要凭主观猜测重定义。

### 7.1 文档同步规则

- 当代码修改影响以下内容时，必须同步更新文档后再结束任务：
  - 对外行为
  - 协议结构或字段语义
  - 配置项、默认值或运行方式
  - UI 交互流程或状态含义
  - 本地通信、IPC、SSE、API 契约
  - 构建、发布、回滚方式

- 当本次任务暴露出新的长期约束、目录边界、验证要求或易错点时，必须同步更新对应目录的 `AGENTS.md` 或 `AGENTS.override.md`。

- 除非任务明确要求只做分析，否则“代码改动完成”不算任务完成；文档同步和说明更新属于交付的一部分。

### 7.2 任务完成定义

任务完成前必须确认：
1. 代码已修改
2. 必要测试已执行或已说明未执行项
3. 相关文档已同步，或明确说明本次无需同步的理由
4. 若新增了可复用规则，已更新对应 `AGENTS.md`

---

## 8. 修改策略

### 8.1 小改动
小改动应满足：

- 尽量不改函数签名
- 尽量不改公共接口
- 不引入额外抽象层
- 不修改无关目录

### 8.2 中等改动
中等改动需要：

- 明确影响模块
- 说明为什么不能只在局部修复
- 说明兼容性影响
- 给出最小验证方案

### 8.3 大改动 / 复杂改动
满足以下任一条件，视为大改动：

- 跨多个模块
- 涉及 `ltfp/`
- 涉及 Agent/Bridge 协议边界
- 涉及认证/安全/证书/会话状态
- 涉及 Tauri 本地通信模型
- 涉及配置结构、消息结构、事件模型
- 涉及构建发布链路

此类任务必须先给出实施思路，再逐步修改。

---

## 9. 测试与验证规则

### 9.1 验证优先顺序
优先执行**最小必要验证**，不要一上来全仓大扫荡。

建议顺序：

1. 目标模块局部检查
2. 目标模块相关测试
3. 目标模块构建验证
4. 有明确必要时再做跨模块验证

### 9.2 Go 模块验证
本仓库含多个 Go module。  
不要默认在仓库根目录直接对所有模块执行同一条命令，而应根据目标模块选择验证方式。

优先按模块执行，例如：

- `cd ltfp && go test ./...`
- `cd agent-core && go test ./...`
- `cd cloud-bridge && go test ./...`
- `cd service-registry && go test ./...`

如涉及可执行入口，可进一步做最小构建验证。

### 9.3 前端 / Tauri 验证
前端项目必须先查看 `package.json` 中实际存在的 scripts，再执行对应命令。  
不要臆造脚本名。

对以下目录适用：

- `apps/dev-agent/`
- `cloud-bridge/web/`

通常优先考虑：

- lint
- test
- build

若为 Tauri 相关改动，应额外关注前端与宿主的联动是否受影响。

### 9.4 未执行的检查必须明说
如果没有运行某个测试、构建或联调验证，必须明确说明：

- 哪些没有跑
- 为什么没跑
- 建议后续由谁补跑什么检查

不得声称“已验证”而实际未执行。

---

## 10. 文档同步规则

若本次修改涉及以下内容，应同步考虑更新文档：

- 对外行为变化
- 协议变化
- 配置变化
- 管理台行为变化
- 本地通信模型变化
- 运行/发布方式变化
- 设计约束变化
- 测试矩阵变化

优先更新：

- `docs/` 下相应技术方案
- `ltfp/docs/` 下测试或发布说明
- 必要时补充 README 或执行清单

---

## 11. 编码风格与实现约束

### 11.1 Go 代码
- 保持现有分层与包边界
- 小接口、清晰职责、显式错误
- 不引入无必要的全局状态
- 不因为“抽象美观”而增加复杂度
- 修改公共签名前必须检查调用点
- 热路径谨慎引入额外分配与重包装

### 11.2 并发与生命周期
对以下问题保持高度敏感：

- goroutine 泄漏
- channel 阻塞
- 锁顺序问题
- context 滥用
- deadline/timeout 语义不清
- 关闭流程和资源回收不完整

### 11.3 前端代码
- 优先复用现有组件与布局
- 保持状态来源清晰
- 不轻易更换已有交互模式
- 不为局部需求大改全局 UI 框架

---

## 12. 提交结果时的输出格式要求

完成任务后，输出应尽量包含以下内容：

1. **目标**：本次修改解决了什么问题
2. **范围**：修改了哪些模块 / 目录 / 文件类别
3. **关键改动**：核心实现点是什么
4. **风险与兼容性**：是否涉及协议、状态机、配置、前后端契约变化
5. **验证情况**：实际执行了哪些检查
6. **未覆盖项**：哪些检查没有执行
7. **后续建议**：需要补的联调、测试或文档项

---

## 13. 明确禁止的行为

代理不得：

- 为了“顺手优化”改无关代码
- 未经要求大规模重命名目录/包/导出类型
- 擅自修改生成产物代替源文件
- 擅自修改 `dist`、`node_modules`、`bin`
- 未确认兼容性就改协议/状态机/消息结构
- 假装执行过测试
- 忽略既有设计文档重新发明方案
- 把本该局部处理的问题扩散为全仓重构

---

## 14. 简化决策准则

遇到选择时，优先级如下：

1. 正确性
2. 兼容性
3. 边界清晰
4. 最小改动
5. 可验证性
6. 美观性 / 抽象性

若“更优雅的方案”会扩大风险或影响面，则优先选择更稳妥的方案。

---

## 15. 对复杂需求的默认流程

对以下任务，默认按“先读文档 → 再分析 → 再修改”的流程执行：

- 协议演进
- Agent / Bridge 协同
- 安全 / 认证 / 证书
- Tauri 本地通信
- 多模块联动
- transport 抽象
- 状态机重构
- 管理台架构调整
- 发布/回滚/兼容性相关变更

必要时，应先给出一个简短执行计划，再进入实现阶段。